@model PaymentTypeViewModel

@{
    ViewData["Title"] = Localizer["Create payment Type"];
}

<div class="card">
    <div class="card-header">
        <h5 class="card-title mb-0">@Localizer["Create New payment Type"]</h5>
    </div>
    <div class="card-body">
        <form asp-action="Create" asp-controller="paymentType" method="post">
            <div class="row mb-4">
                <div class="col-md-6">
                    <div class="form-group mb-3">
                        <label asp-for="TypeShortName" class="form-label">@Localizer["Arabic Name"]</label>
                        <input asp-for="TypeShortName" class="form-control" placeholder="@Localizer["Enter payment type name in Arabic"]" />
                        <span asp-validation-for="TypeShortName" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group mb-3">
                        <label asp-for="TypeName" class="form-label">@Localizer["English Name"]</label>
                        <input asp-for="TypeName" class="form-control" placeholder="@Localizer["Enter payment type name in English"]" />
                        <span asp-validation-for="TypeName" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <hr />
            
            <div class="row">
                <div class="col-md-6">
                    <a asp-action="Index" class="btn btn-secondary w-100">
                        <i class="fas fa-arrow-left me-1"></i> @Localizer["Back to List"]
                    </a>
                </div>
                <div class="col-md-6">
                    <button type="submit" class="btn btn-primary w-100">
                        <i class="fas fa-save me-1"></i> @Localizer["Create"]
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}